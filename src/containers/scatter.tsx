import React from "react";
import { useRecoilState, useRecoilValue } from "recoil";
import {} from "../state";

interface IScatterProps {}

export const Scatter: React.FunctionComponent<IScatterProps> = ({}) => {
  return (
    <div className="container" id="container-scatter">
      scatter
    </div>
  );
};
