import React from "react";
import { useRecoilState, useRecoilValue } from "recoil";
import {} from "../state";

interface IStatsProps {}

export const Stats: React.FunctionComponent<IStatsProps> = ({}) => {
  return (
    <div className="container" id="container-stats">
      stats
    </div>
  );
};
